<!DOCTYPE html>
<html lang="en">
  <head>
    <meta charset="UTF-8" />
    <meta name="viewport" content="width=device-width, initial-scale=1.0" />
    <title>Nyesco | Edit About</title>
    <link
      href="https://cdn.quilljs.com/1.3.6/quill.snow.css"
      rel="stylesheet"
    />
    <script src="https://cdn.tailwindcss.com"></script>
    <script>
      tailwind.config = {
        theme: {
          extend: {
            colors: {
              nyesco: {
                dark: '#0f172a',
                accent: '#f8fafc',
                muted: '#64748b',
                border: '#334155',
              },
            },
          },
        },
      };
    </script>

    <style>
      /* Overriding Quill's default theme to fit the Nyesco dark aesthetic */
      .ql-toolbar.ql-snow {
        @apply bg-slate-100 border-none rounded-t-lg;
      }
      .ql-container.ql-snow {
        @apply border-none bg-white text-slate-900 rounded-b-lg;
      }
      .social-group input::placeholder {
        @apply text-nyesco-muted/50;
      }
    </style>
  </head>

  <body
    class="bg-nyesco-dark text-nyesco-accent font-sans antialiased min-h-screen"
  >
    <%- include('../../partials/admin-navbar') %>

    <div class="max-w-5xl mx-auto px-6 py-12">
      <div class="flex items-center gap-6 mb-12">
        <h1 class="text-4xl font-bold tracking-tighter uppercase italic">
          Edit About
        </h1>
      </div>

      <section
        class="bg-slate-900/40 border border-nyesco-border p-8 rounded-sm mb-10 relative overflow-hidden"
      >
        <div class="absolute top-0 left-0 w-1 h-full bg-nyesco-accent"></div>
        <h2
          class="text-xs font-black uppercase tracking-[0.3em] text-nyesco-muted mb-8"
        >
          Identity & Bio
        </h2>

        <form
          id="aboutForm"
          action="/about/edit"
          method="POST"
          class="space-y-6"
        >
          <div id="editor-container" class="h-80 shadow-2xl">
            <%- about.description %>
          </div>
          <input type="hidden" name="description" id="description-input" />

          <div class="flex justify-end">
            <button
              type="submit"
              class="bg-nyesco-accent text-nyesco-dark px-10 py-3 font-black uppercase tracking-widest text-xs hover:bg-white transition-all transform hover:-translate-y-1"
            >
              Save Bio
            </button>
          </div>
        </form>
      </section>

      <section
        class="bg-slate-900/40 border border-nyesco-border p-8 rounded-sm shadow-xl"
      >
        <h2
          class="text-xs font-black uppercase tracking-[0.3em] text-nyesco-muted mb-8"
        >
          Digital Footprint
        </h2>

        <form action="/about/socials" method="POST" class="space-y-8">
          <div id="social-fields" class="grid grid-cols-1 md:grid-cols-2 gap-6">
            <% socials.forEach(function(s){ %>
            <div
              class="group bg-nyesco-dark/50 border border-nyesco-border p-6 rounded-sm relative"
            >
              <input
                name="platform[]"
                value="<%= s.platform %>"
                placeholder="PLATFORM"
                class="w-full bg-transparent border-b border-nyesco-border py-2 mb-4 focus:border-nyesco-accent outline-none text-sm font-bold tracking-wide"
              />
              <input
                name="link[]"
                value="<%= s.link %>"
                placeholder="PROFILE URL"
                class="w-full bg-transparent border-b border-nyesco-border py-2 focus:border-nyesco-accent outline-none text-xs text-nyesco-muted"
              />
              <button
                type="button"
                onclick="removeSocial(this)"
                class="mt-4 text-[10px] uppercase font-bold text-red-500 hover:text-red-400 tracking-tighter opacity-0 group-hover:opacity-100 transition-opacity"
              >
                [ Remove Entry ]
              </button>
            </div>
            <% }) %>
          </div>

          <div
            class="flex items-center justify-between pt-6 border-t border-nyesco-border"
          >
            <button
              type="button"
              id="add-social"
              class="text-xs font-bold uppercase tracking-widest border border-nyesco-muted px-4 py-2 hover:border-nyesco-accent hover:text-nyesco-accent transition-all"
            >
              + Add Social
            </button>
            <button
              type="submit"
              class="bg-nyesco-accent text-nyesco-dark px-10 py-3 font-black uppercase tracking-widest text-xs hover:bg-white transition-all transform hover:-translate-y-1 shadow-[4px_4px_0px_0px_rgba(51,65,85,1)]"
            >
              Update Social Media
            </button>
          </div>
        </form>
      </section>
    </div>

    <script src="https://cdn.quilljs.com/1.3.6/quill.js"></script>
    <script>
      var quill = new Quill('#editor-container', {
        theme: 'snow',
        modules: {
          toolbar: [
            [{ header: [1, 2, 3, false] }],
            ['bold', 'italic'],
            ['link', 'blockquote', 'code-block'],
            [{ list: 'ordered' }, { list: 'bullet' }],
          ],
        },
      });

      var form = document.getElementById('aboutForm');
      form.onsubmit = function () {
        var descriptionInput = document.getElementById('description-input');
        descriptionInput.value = quill.root.innerHTML;
        return true;
      };

      const container = document.getElementById('social-fields');
      document
        .getElementById('add-social')
        .addEventListener('click', function () {
          const wrapper = document.createElement('div');
          wrapper.className =
            'group bg-nyesco-dark/50 border border-nyesco-border p-6 rounded-sm relative';
          wrapper.innerHTML = `
            <input name="platform[]" placeholder="PLATFORM" class="w-full bg-transparent border-b border-nyesco-border py-2 mb-4 focus:border-nyesco-accent outline-none text-sm font-bold tracking-wide" />
            <input name="link[]" placeholder="PROFILE URL" class="w-full bg-transparent border-b border-nyesco-border py-2 focus:border-nyesco-accent outline-none text-xs text-nyesco-muted" />
            <button type="button" onclick="removeSocial(this)" class="mt-4 text-[10px] uppercase font-bold text-red-500 hover:text-red-400 tracking-tighter opacity-0 group-hover:opacity-100 transition-opacity">
              [ Remove Entry ]
            </button>
        `;
          container.appendChild(wrapper);
        });

      function removeSocial(btn) {
        btn.parentElement.remove();
      }
    </script>
  </body>
</html>
